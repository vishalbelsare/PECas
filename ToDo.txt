ToDo:

- Berechnung Kovarianzmatrix überprüfen
- Ergebnis muss das gleiche sein für Berechnungsweise Seite 24 Skript SI und die J_plus-Formel
- Testen ob Berechnung mit und ohne Gleichungsnebenbedingungen funktioniert, indem weitere Variable x in Schätzfunktion, und dafür Gleichungsbedingung z. B. x = 3 eingeführt wird

- Anbindung Rawesome
- Plot der Konfidenzellipsoide


Auftretende Fehler:

- Aufruf von generate_pseudo_measurement_data() ohne vorhandenes xtrue wird nicht abgefangen
- "try-set-values"-messages apper also if the get-fcuntiosn are only used for checking
- vereinfachte Berechnung der Kovarianzmatrix fehlerhaft
- Skalierung mit sigma der Messwerte und Modellfunktion fehlt noch komplett